{% extends 'base.html.twig' %}

{% block title %}FICHE DE LA CATEGORIE{% endblock %}

{% block body %}
<h1 class="text-center pt-2">FICHE DE LA CATEGORIE</h1>

<div class="row">
            <div class="card mb-3 container">
                <h4 class="card-header">{{ laCategorie.libelle}}</h4>
                <div class="row">
                    <div class="col-2 d-fmlex align-items-center">
                        <img class="avatar mx-auto my-3" style="height: auto; max-width: 200px; display: block;" src=" {{asset(laCategorie.image)}}" alt="Card image">
                    </div>
                    <div class="col">
                        <ul class="list-group list-group-flush">
                        <li class="list-group-item">{{laCategorie.description}}</li>>
                        
                </ul>
                    </div>
                </div>
            </div>
        </div>
        <a href="{{path('categories')}}" class="btn bts-success btn-block text-center">Revenir a la liste </a>
</div>

{% endblock %}
